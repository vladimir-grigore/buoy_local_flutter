import 'package:flutter/material.dart';

class FeedbackPage extends StatelessWidget {
  FeedbackPage();

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("FeedbackPage placeholder"),
    );
  }
}